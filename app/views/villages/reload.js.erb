$('#users').html('<%= j(render @users) %>')
change_button('<%= j(current_user.action_type) %>')
<% if current_user.job_id == 1 && current_user.action_type == 'night' &&@tweet%>
aim('<%= j(@tweet.content) %>')
<% end %>
